import 'package:flutter/cupertino.dart';

class el_khatma_screen extends StatelessWidget {
  const el_khatma_screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("el-katma")),
    );
  }
}

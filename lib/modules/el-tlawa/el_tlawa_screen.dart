import 'package:flutter/cupertino.dart';

class el_tlawa_screen extends StatelessWidget {
  const el_tlawa_screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("el-tlawa")),

    );
  }
}
